{
  "Comment": "adding subdomain for new server",
  "Changes": [
  {
    "Action": "UPSERT",
    "ResourceRecordSet": {
	    "Name": "<%= @host %>.<%= @domain %>",
       "Type": "CNAME",
       "TTL": 600,
        "ResourceRecords": [
          {
			      "Value": "<%= @ip %>"
          }
        ]
      }
    }
  ]
}
